export default function Header() {
    return (
        <header className = "app-header">
            <h1> Serverless Task App</h1>
            <p> A React frontend for the AWS serverless task API.</p>
        </header>
    );
}